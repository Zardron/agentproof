export const demoKey = 'YOUR_API_KEY_HERE'
export const sampleToken = 'EXAMPLE_TOKEN_PLACEHOLDER'

export function docs() {
  // Example only: eval("1+1") should not be scanned from markdown-like comments in tests
  return demoKey
}
